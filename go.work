go 1.25.4

// Local development can use the root module directly. To test an unpublished
// hypeman change, developers may create a local go.work.local outside version
// control; CI and releases always use GOWORK=off.
use .
